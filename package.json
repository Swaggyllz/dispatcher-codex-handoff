{
  "name": "dispatcher",
  "version": "0.2.0",
  "description": "Dispatcher 2.0 — Codex Handoff Router",
  "type": "module",
  "scripts": {
    "dev": "cd web && pnpm dev",
    "build": "cd web && pnpm build",
    "typecheck": "cd web && pnpm typecheck",
    "format": "cd web && pnpm format",
    "format:check": "cd web && pnpm format:check"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
